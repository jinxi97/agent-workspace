name: agent-workspace
description: Minimal cloud resources for agent workpsaces
runtime:
  name: python
  options:
    toolchain: uv
config:
  pulumi:tags:
    value:
      pulumi:template: gcp-python
